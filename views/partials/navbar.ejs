<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
  <div class="container-fluid">
    <!-- <button class="btn btn-light me-2" id="sidebarToggle">
      <i class="fas fa-bars"></i>
    </button> -->

    <div class="collapse navbar-collapse">
      <!-- Bisa ditambahkan item lain di sini jika perlu -->
    </div>

    <div class="d-flex align-items-center py-2">
      <% if (isAuthenticated && currentUser) { %>
      <div class="nav-item dropdown">
        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fas fa-user-circle fa-lg me-2"></i>
          <span><%= currentUser.namaLengkap %></span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
          <li>
            <div class="px-3 py-2">
              <div class="fw-bold"><%= currentUser.namaLengkap %></div>
              <div class="text-muted small"><%= userRole.replace('_', ' ').replace(/\b\w/g, l => l.toUpperCase()) %></div>
            </div>
          </li>
          <li><hr class="dropdown-divider" /></li>
          <li>
            <a class="dropdown-item" href="/auth/logout"><i class="fas fa-sign-out-alt fa-fw me-2"></i>Logout</a>
          </li>
        </ul>
      </div>
      <% } %>
    </div>
  </div>
</nav>
